apiVersion: v1
kind: Service
metadata:
  name: web-server-service
  namespace: fraud-detection
spec:
  selector:
    app: web-server
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: LoadBalancer  # Or ClusterIP if internal
